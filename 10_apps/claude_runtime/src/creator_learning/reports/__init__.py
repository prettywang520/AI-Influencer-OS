"""Report dataclasses (models.py), pure builders (builder.py), and
Markdown renderers (markdown.py) for the Creator Learning Engine. The
seventh requested report -- the full Creator DNA record -- is not
re-built here; it is already fully representable via
creator_intelligence.serialization and is exported directly by
engine.py alongside these six.
"""
